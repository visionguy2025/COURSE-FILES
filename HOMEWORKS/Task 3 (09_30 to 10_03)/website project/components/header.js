// HEADER COMPONENT
const Header = () => {
    // SELECT HEADER TAG
    const headerContainer = document.querySelector('.header')

    // CREATE HEADER
    const headerElement = `
        <div> HEADER </div>
    `
    
    // ASSIGN HEADER ELEMENT TO HEADER TAG OF THE PAGE
    // headerContainer.innerHTML = headerElement
}
