// MOBILE HEADER
const MobileHeader = (directory) => {
    let headerInnerHTML = `
        <div>
            JUST CREATE NEW NAVBAR, HIDE DESKTOP, SHOW MOBILE
        </div>
    `

    document.querySelector('#header').innerHTML = headerInnerHTML
}
